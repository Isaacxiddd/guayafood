# Sesión — 10 de Junio 2026

## Decisiones de arquitectura

### 1. Icono oficial de Mercado Pago
Reemplazar SVGs genéricos (hearts) por el icono oficial de MP en botones de pago.

**Archivos:** `OrderModal.astro`, `PaymentButton.astro`

---

### 2. Instagram handle
`@guayafood.ba` → `@guayafoodba`

**Archivo:** `src/lib/config.ts`

---

### 3. Imagen real de empanaditas
Se agregó `public/empanadas.jpeg` a la galería y productos, reemplazando el emoji 🥟.

**Archivos:** `Galeria.astro`, `Productos.astro`, `config.ts`

---

### 4. Seguridad — precios del lado del servidor
El cliente ahora solo envía `productId` + `quantity`. El servidor resuelve nombre, precio unitario y total desde `PRODUCT_CATALOG`.

**Archivos:** `create-preference.ts`, `save-order.ts`, `index.astro`, `OrderModal.astro`, `PaymentButton.astro`, `Combos.astro`, `Productos.astro`, `config.ts`

---

### 5. Origin validation
Todos los endpoints POST validan el header `Origin` contra `PUBLIC_SITE_URL`.

**Archivo:** `rate-limit.ts` (nueva función `checkOrigin`)

---

### 6. Price validation server-side
`create-preference.ts` rechaza cualquier producto que no esté en `PRODUCT_CATALOG`.

---

### 7. Secret exposure — limpieza de historial
Se usó `git filter-branch` para purgar credenciales de Mercado Pago del historial de git. Force-push a `main` y `test`.

---

### 8. CI/CD fixes
- Eliminado `version: 11` duplicado de `pnpm/action-setup` (se lee de `package.json`)
- Actualizado `actions/checkout@v5` y `actions/setup-node@v5`

---

### 9. Gitleaks
Verificado que el escaneo de secrets pase correctamente en CI.

---

## Pendientes

- **Idempotencia en memoria** — no escala entre instancias de Vercel. Migrar a algo persistente si crece el volumen.
- **Tests de Playwright** — fallan en CI por el webServer (problema preexistente).
- **Reintento save-order** — si falla el guardado en Sheets, el pedido puede perderse.

---

> Para próximas sesiones, crear un archivo `sesion-YYYY-MM-DD.md` en esta misma carpeta.
